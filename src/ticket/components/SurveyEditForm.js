import React from "react";

/**
 * Parent:
 *    WorkOrderPage
 */
const SurveyEditForm = (props) => {
  console.log(
    "🚀 ~ file: SurveyEditForm.js ~ line 4 ~ SurveyEditForm ~ props",
    props
  );
  const { data, editSurveyLoading, onEditComplete, handleSurveyDetailsCancel } =
    props;
  return <div>SurveyEditForm</div>;
};

export default SurveyEditForm;
