import { Box, Typography } from "@mui/material";
import React from "react";

const UserRegionSelect = () => {
  return (
    <Box>
      <Typography variant="h4">User Region Assignment form</Typography>
    </Box>
  );
};

export default UserRegionSelect;
