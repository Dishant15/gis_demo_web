import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1881bc",
    },
    secondary: {
      main: "#b98919",
    },
    background: {
      default: "#efefef",
    },
  },
});
