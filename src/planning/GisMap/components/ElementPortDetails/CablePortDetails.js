import { Table, TableHead } from "@mui/material";
import React from "react";

const CablePortDetails = ({ portDetails }) => {
  return (
    <Table>
      <TableHead>Table from data</TableHead>
    </Table>
  );
};

export default CablePortDetails;
